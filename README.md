<p align="center">
<a href="http://nestjs.com/" target="blank"><img src="https://nestjs.com/img/logo-small.svg" width="120" alt="Nest Logo" /></a>
</p>

🏎️ Buriram Go! - Smart Tourism & Speed City
เว็บแอปพลิเคชันแนะนำสถานที่ท่องเที่ยวและกิจกรรมในจังหวัดบุรีรัมย์ พัฒนาด้วย NestJS เชื่อมต่อข้อมูลจริงจาก TAT API, RCBT Data Center และระบบ AI Chatbot (น้องรุ้ง)

🌟 ฟีเจอร์ที่สำคัญ (Key Features)

AI Chatbot (น้องรุ้ง): ระบบตอบโต้ผ่าน Dialogflow และ Gemini/Groq API 


Real-time API Integration: เชื่อมต่อข้อมูลที่พัก ร้านอาหาร และเส้นทางท่องเที่ยวจาก TAT และ RCBT Data Center 


News System: ระบบข่าวสารดึงจาก Google Sheets ผ่าน OpenSheet URL 

Racing Splash Screen: หน้าต้อนรับที่ปรับแต่งมาเพื่อชาวบุรีรัมย์โดยเฉพาะ

🛠️ การตั้งค่าโปรเจกต์ (Project Setup)
1. ติดตั้ง Dependencies
Bash
$ npm install
2. ตั้งค่าสภาพแวดล้อม (Environment Variables)
คัดลอกไฟล์ .env.example เป็น .env และกรอกข้อมูลให้ครบถ้วน:


Port: กำหนดเป็น 3080 (ตามที่ตั้งค่าไว้ใน main.ts) 


AI Keys: ใส่ GOOGLE_API_KEY และ GROQ_API_KEY ให้เรียบร้อย 


Dialogflow: นำเนื้อหา JSON Key ทั้งหมดมาวางใน DIALOGFLOW_PRIVATE_KEY โดยครอบด้วยเครื่องหมาย Single Quote (') 

🚀 การรันโปรเจกต์ (Run the Project)
สำหรับการพัฒนา (Development Mode)
ใช้คำสั่งนี้เพื่อรันแอปพร้อมระบบ Hot Reload:

Bash
$ npm run start:dev
สำหรับการใช้งานจริง (Build & Production Mode)
เมื่อต้องการใช้งานบน Server จริง ให้ทำการ Build เป็น Javascript ก่อน:

Bash
# 1. Build โปรเจกต์ (ผลลัพธ์จะอยู่ที่โฟลเดอร์ /dist)
$ npm run build

# 2. รันแอปในโหมด Production
$ npm run start:prod

การเข้าใช้งาน: เปิดเบราว์เซอร์ไปที่ http://localhost:3080 

📁 โครงสร้างไฟล์ที่สำคัญ (Project Structure)

src/main.ts: จุดเริ่มต้นของเซิร์ฟเวอร์ (รันบน Port 3080) 


src/app.service.ts: ตัวจัดการ Dialogflow และการดึงข้อมูล AI 

views/: หน้าจอแสดงผล (EJS Templates) เช่น หน้า Home และ Detail


.env.example: ไฟล์ตัวอย่างการตั้งค่า Environment ทั้งหมด 

⚠️ ข้อควรระวัง (Important Notes)
Security: อย่าลืมตรวจสอบว่าไฟล์ .env ถูกเพิ่มลงใน .gitignore แล้ว เพื่อความปลอดภัยของ API Keys

Node.js: แนะนำให้ใช้เวอร์ชัน v24 (LTS)


API Endpoints: ตรวจสอบว่า TAT_PROVINCE_ID และ provinceId ใน RCBT ถูกตั้งค่าเป็นบุรีรัมย์แล้ว 

Developed with ❤️ for Buriram Smart City