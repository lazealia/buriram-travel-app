<!DOCTYPE html>
<html lang="th">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
    <title><%= appName %></title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Kanit:wght@300;400;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    
    <style>
        body { font-family: 'Kanit', sans-serif; }
        .no-scrollbar::-webkit-scrollbar { display: none; }
        .news-gradient { background: linear-gradient(180deg, rgba(79, 70, 229, 0) 30%, rgba(0, 0, 0, 0.85) 100%); }
        
        /* สไตล์ Splash Screen ใหม่: Speed & Rainbow */
        #splash-screen {
            position: fixed;
            inset: 0;
            z-index: 9999;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            background: radial-gradient(circle at center, #6366f1 0%, #4f46e5 100%);
        }

        .splash-played #splash-screen { display: none !important; }

        .logo-container {
            position: relative;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .logo-circle {
            width: 100px;
            height: 100px;
            background: white;
            border-radius: 35px;
            display: flex;
            align-items: center;
            justify-content: center;
            box-shadow: 0 20px 40px rgba(0,0,0,0.2);
            animation: speed-in 0.8s cubic-bezier(0.175, 0.885, 0.32, 1.275);
        }

        .rainbow-track {
            position: absolute;
            width: 140px;
            height: 140px;
            border: 4px solid transparent;
            border-top-color: #fbbf24;
            border-right-color: #ef4444;
            border-radius: 50%;
            animation: spin 2s linear infinite;
            opacity: 0.4;
        }

        @keyframes speed-in {
            0% { transform: translateX(-200%) rotate(-20deg); opacity: 0; }
            100% { transform: translateX(0) rotate(0deg); opacity: 1; }
        }

        @keyframes spin { to { transform: rotate(360deg); } }

        .brand-text {
            margin-top: 24px;
            text-align: center;
            animation: fade-up 1s ease-out;
        }

        @keyframes fade-up {
            0% { transform: translateY(20px); opacity: 0; }
            100% { transform: translateY(0); opacity: 1; }
        }

        .is-loading { overflow: hidden; height: 100vh; }
        .splash-fade-out { opacity: 0; pointer-events: none; transition: all 0.6s cubic-bezier(0.4, 0, 0.2, 1); transform: scale(1.1); }
    </style>

    <script>
        if (sessionStorage.getItem('splashPlayed')) {
            document.documentElement.classList.add('splash-played');
        }
    </script>
</head>
<body class="bg-gray-50 pb-28">

    <div id="splash-screen">
        <div class="logo-container">
            <div class="rainbow-track"></div>
            <div class="logo-circle">
                <i class="fa-solid fa-rainbow text-5xl bg-gradient-to-br from-indigo-500 via-purple-500 to-pink-500 bg-clip-text text-transparent"></i>
            </div>
        </div>
        <div class="brand-text">
            <h1 class="text-white text-3xl font-black tracking-tight italic uppercase">BURIRAM <span class="text-yellow-400">GO!</span></h1>
            <div class="flex items-center justify-center gap-2 mt-2">
                <span class="h-[1px] w-6 bg-white/30 rounded-full"></span>
                <p class="text-[9px] text-indigo-100 font-bold uppercase tracking-[0.2em]">Sport & Speed City</p>
                <span class="h-[1px] w-6 bg-white/30 rounded-full"></span>
            </div>
        </div>
        <div class="absolute bottom-16 w-32 h-1.5 bg-white/10 rounded-full overflow-hidden border border-white/5">
            <div class="h-full bg-gradient-to-r from-yellow-400 to-orange-500 w-full origin-left animate-[loading_1.8s_ease-in-out_infinite]"></div>
        </div>
    </div>

    <style>
        @keyframes loading {
            0% { transform: scaleX(0); }
            50% { transform: scaleX(1); transform-origin: left; }
            51% { transform-origin: right; }
            100% { transform: scaleX(0); transform-origin: right; }
        }
    </style>

    <header class="bg-indigo-600 px-6 pt-10 pb-14 rounded-b-[45px] shadow-lg relative">
        <div class="flex justify-between items-center text-white">
            <div class="flex items-center gap-3">
                <div class="w-10 h-10 bg-white/20 rounded-xl flex items-center justify-center backdrop-blur-md border border-white/30">
                    <i class="fa-solid fa-rainbow text-lg"></i>
                </div>
                <div>
                    <h1 class="text-lg font-bold leading-none uppercase">BURIRAM GO</h1>
                    <p class="text-[9px] text-indigo-200 font-medium mt-1">Sport & Speed City</p>
                </div>
            </div>
        </div>
        
        <form action="/" method="GET" class="absolute -bottom-6 left-6 right-6 bg-white rounded-2xl shadow-xl flex items-center px-5 py-3.5 border border-gray-100">
            <i class="fa-solid fa-magnifying-glass text-indigo-400 mr-3 text-sm"></i>
            <input type="text" name="search" placeholder="ค้นหากิจกรรม มาราธอน แข่งรถ..." class="bg-transparent border-none outline-none text-xs w-full text-gray-600 font-medium">
        </form>
    </header>

    <main class="mt-10 px-6">
        <div class="mb-8">
            <h2 class="font-bold text-gray-800 text-base mb-4">กิจกรรมยอดฮิต</h2>
            <div class="flex space-x-4 overflow-x-auto no-scrollbar py-1">
                <% if (news && news.length > 0) { %>
                    <% news.slice(0, 3).forEach(function(item) { %>
                        <div onclick="location.href='/news/<%= item.id %>'" class="min-w-[280px] h-44 bg-gray-200 rounded-[30px] relative overflow-hidden shadow-md active:scale-95 transition-transform cursor-pointer">
                            <img src="<%= item.img %>" class="w-full h-full object-cover" onerror="this.src='https://via.placeholder.com/400x300'">
                            <div class="absolute inset-0 news-gradient flex flex-col justify-end p-5">
                                <span class="bg-white/20 backdrop-blur-md text-white text-[8px] px-2 py-0.5 rounded-md w-fit mb-2 border border-white/30"><%= item.category %></span>
                                <h3 class="text-white font-bold text-sm line-clamp-2 leading-tight"><%= item.title %></h3>
                            </div>
                        </div>
                    <% }); %>
                <% } %>
            </div>
        </div>

        <h2 class="font-bold text-gray-800 text-base mb-4">ข่าวสารล่าสุด</h2>
        <div class="space-y-4">
            <% if (news && news.length > 0) { %>
                <% news.forEach(function(item) { %>
                    <div onclick="location.href='/news/<%= item.id %>'" class="bg-white p-3 rounded-[30px] shadow-sm border border-gray-100 flex items-center gap-4 active:bg-gray-50 transition-colors cursor-pointer">
                        <img src="<%= item.img %>" class="w-20 h-20 rounded-[22px] object-cover shadow-sm">
                        <div class="flex-1 min-w-0">
                            <div class="flex items-center gap-2 mb-1 text-[9px]">
                                <span class="font-bold text-indigo-500 uppercase"><%= item.category %></span>
                                <span class="text-gray-400"><%= item.date %></span>
                            </div>
                            <h3 class="font-bold text-gray-800 text-[11px] line-clamp-2 leading-snug"><%= item.title %></h3>
                        </div>
                    </div>
                <% }); %>
            <% } %>
        </div>
    </main>

    <%- include('partials/nav', { currentPage: 'home' }) %>

    <script>
        (function() {
            const splash = document.getElementById('splash-screen');
            const isPlayed = sessionStorage.getItem('splashPlayed');

            if (isPlayed) {
                if (splash) splash.remove();
            } else {
                document.body.classList.add('is-loading');
                window.addEventListener('load', function() {
                    setTimeout(() => {
                        if (splash) {
                            splash.classList.add('splash-fade-out');
                            sessionStorage.setItem('splashPlayed', 'true');
                            setTimeout(() => { 
                                splash.remove(); 
                                document.body.classList.remove('is-loading'); 
                            }, 600);
                        }
                    }, 1800);
                });
            }
        })();
    </script>
</body>
</html>