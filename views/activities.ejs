<!DOCTYPE html>
<html lang="th">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= appName %></title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Kanit:wght@300;400;700&display=swap" rel="stylesheet">
    <style> body { font-family: 'Kanit', sans-serif; } </style>
</head>
<body class="bg-gray-50 pb-28">

    <header class="bg-indigo-600 px-5 pt-8 pb-4 rounded-b-[30px] shadow-lg sticky top-0 z-50">
        <div class="flex flex-col gap-3">
            <div class="flex items-center gap-3">
                <a href="/services" class="w-8 h-8 flex items-center justify-center bg-white/20 rounded-lg text-white backdrop-blur-sm active:scale-90 transition-transform">
                    <i class="fa-solid fa-chevron-left text-sm"></i>
                </a>
                <div class="flex-shrink-0">
                    <h1 class="text-base font-black text-white leading-none tracking-tight">กิจกรรมชุมชน</h1>
                    <p class="text-[9px] text-indigo-200 font-medium uppercase tracking-tighter mt-1">Community Experiences</p>
                </div>
                <div class="flex-1 bg-white/10 backdrop-blur-md rounded-xl p-1 flex items-center border border-white/20">
                    <div class="w-7 h-7 flex items-center justify-center text-indigo-100">
                        <i class="fa-solid fa-magnifying-glass text-[10px]"></i>
                    </div>
                    <input type="text" id="activitySearch" onkeyup="filterActivities()" 
                           placeholder="ค้นหาชื่อกิจกรรม หรือชุมชน..." 
                           class="flex-1 bg-transparent border-none focus:ring-0 text-xs text-white placeholder:text-indigo-200/60 py-1">
                </div>
            </div>
        </div>
    </header>

    <main id="activityContainer" class="p-4 space-y-6">
        <% if (activities && activities.length > 0) { %>
            <% activities.forEach(function(item) { %>
                <div class="activity-card bg-white rounded-[35px] overflow-hidden shadow-sm border border-gray-100 group transition-all active:scale-[0.98]">
                    <div class="h-52 relative">
                        <img src="<%= item.serviceImage %>" 
                             class="w-full h-full object-cover" 
                             onerror="this.src='https://images.unsplash.com/photo-1533900298318-6b8da08a523e?w=500&q=80'">
                        
                        <% if(item.serviceGroup) { %>
                        <div class="absolute top-4 left-4 bg-white/90 backdrop-blur-md px-3 py-1 rounded-full shadow-sm">
                            <span class="text-[10px] font-bold text-indigo-600 uppercase">
                                <i class="fa-solid fa-star mr-1"></i> <%= item.serviceGroup.GroupName %>
                            </span>
                        </div>
                        <% } %>
                    </div>

                    <div class="p-5">
                        <h3 class="text-lg font-bold text-gray-800 leading-tight mb-2">
                            <%= item.serviceName %>
                        </h3>
                        
                        <div class="space-y-1">
                            <div class="flex items-center text-gray-400 text-[11px]">
                                <i class="fa-solid fa-users mr-2 text-indigo-400"></i>
                                <span class="truncate font-medium"><%= item.community.community_name %></span>
                            </div>
                            <div class="flex items-center text-gray-400 text-[11px]">
                                <i class="fa-solid fa-location-dot mr-2 text-red-400"></i>
                                <span>ต.<%= item.community.tambon_name.trim() %> อ.<%= item.community.amphur_name.trim() %> จ.<%= item.community.province_name %></span>
                            </div>
                        </div>

                        <div class="flex items-center justify-between pt-4 mt-4 border-t border-dashed border-gray-100">
                            <div class="flex flex-col">
                                <span class="text-[9px] text-gray-400 uppercase font-bold tracking-widest">ราคา</span>
                                <span class="text-lg font-black text-indigo-600">
                                    <%= (item.servicePrice && item.servicePrice > 0) ? '฿' + item.servicePrice : 'ติดต่อสอบถาม' %>
                                </span>
                            </div>
                            
                            <a href="/activity-detail/<%= item.serviceID %>" 
                                class="bg-indigo-600 text-white px-5 py-2 rounded-2xl text-xs font-bold flex items-center shadow-lg shadow-indigo-100">
                                ดูข้อมูลกิจกรรม <i class="fa-solid fa-chevron-right ml-2 text-[8px]"></i>
                            </a>
                        </div>
                    </div>
                </div>
            <% }); %>
        <% } %>
        
        <div id="noSearch" class="hidden flex flex-col items-center justify-center py-20 text-center">
            <i class="fa-solid fa-calendar-minus text-gray-200 text-6xl mb-4"></i>
            <p class="text-gray-400 font-bold">ไม่พบกิจกรรมที่ต้องการ</p>
        </div>
    </main>

    <%- include('partials/nav', { currentPage: 'services' }) %>

    <script>
    function filterActivities() {
        const query = document.getElementById('activitySearch').value.toLowerCase().trim();
        const cards = document.getElementsByClassName('activity-card');
        const noSearch = document.getElementById('noSearch');
        let hasResults = false;

        Array.from(cards).forEach(card => {
            const content = card.innerText.toLowerCase();
            if (content.includes(query)) {
                card.style.display = "block";
                hasResults = true;
            } else {
                card.style.display = "none";
            }
        });

        if (noSearch) {
            noSearch.style.display = (hasResults || query === "") ? "none" : "flex";
        }
    }
    </script>
</body>
</html>